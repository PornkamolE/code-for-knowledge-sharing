package constants

import "github.com/gofiber/fiber/v2"

const (
	LanguageHeader = fiber.HeaderAcceptLanguage
	DeviceIdHeader = "device-id"
)

const (
    RefreshToken = "REFRESH_TOKEN"
    BearerToken  = "BEARER_TOKEN"
)

const (
    UserIdKey   = "userId"
    UsernameKey = "username"
)